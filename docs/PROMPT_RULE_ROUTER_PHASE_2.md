# Prompt Rule Router — Phase 2

Phase 2 adds an opt-in conflict resolver for dialogue treatment and visible text. Menu selections and input provenance remain authoritative. `source_script` existence alone never selects verbatim mode. The legacy prompt remains unchanged while `PROMPT_RULE_ROUTER_V2` is disabled.
